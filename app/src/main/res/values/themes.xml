<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BTLG05" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/key</item>
        <item name="colorPrimaryVariant">@color/key</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/key</item>
        <item name="android:windowBackground">#FFFFFF</item> <!-- Màu nền trắng -->
        <item name="colorButtonNormal">@color/key</item> <!-- Màu nút -->
        <!-- Customize your theme here. -->
    </style>
</resources>